#maybe a plotphase methods? And then a plot method that just calls the plotmag and plotphase methods?
#not clear if this is needed, if there are no additional features the method could just be 
#inherited from tts